name: health-check
on:
  push:
  pull_request:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run health-check
        run: |
          set -e
          bash .trae/health-check.sh
